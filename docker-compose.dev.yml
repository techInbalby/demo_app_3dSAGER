services:
  app:
    build: .
    ports:
      - "3000:5000"
    volumes:
      - .:/app
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    restart: unless-stopped
    command: ["python", "app.py"]
